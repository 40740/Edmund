name: Debug

on:
  push:
    branches: [main]
    paths: ['.github/workflows/debug.yml']
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-15
    steps:
      - name: A
        run: echo "step A ok"
