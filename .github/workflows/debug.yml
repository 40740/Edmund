name: Debug

on:
  push:
    branches: [main]
    paths: ['.github/workflows/debug.yml']
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-15
    steps:
      - name: A
        run: echo "step A ok"
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/cache@v5
        with:
          path: .build
          key: debug-${{ runner.os }}
